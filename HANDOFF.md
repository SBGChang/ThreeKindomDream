# 灰盒 v0 交接

> **狀態**：可玩。帳下篇（1 章）＋ 魏線（3 章）＝ 32 回合完整 Run，含結算與天命商店閉環。
> **大檢定已改為七關戰役**（[RFC-01](docs/RFC-01-campaign-rework.md)）——
> 見下方「戰役制與養成兌現」。
> **四道門禁全過**：typecheck ／ discipline（56 檔）／ content ／ modules（77 項）。

---

## 跑起來

**Windows：雙擊 `start.bat`**（選單式：啟動遊戲／門禁／模擬器／校準報告）。
首次執行會自動 `npm install` 與編譯內容產物。

手動指令：

```bash
npm install
npm run content:build     # 作者層 → 產物（已提交，只有改內容時才需要）
npm run verify            # 四道門禁
npm run dev               # http://localhost:5173
```

分析工具：

```bash
npm run sim -- 300        # 七種策略各跑 300 次，輸出分佈
npm run sim -- 300 focused
tsx scripts/smoke.ts 4242   # 單輪逐回合明細
tsx scripts/calibrate.ts 150  # 各章檢定值分佈（DC 校準用）
```

---

## 目前的範圍

| 有 | 沒有 |
|---|---|
| **單動作回合**（鍛鍊 4 格 ＋ 事件 0–3 個，擇一即推進） | 技能系統（架構已備，內容未寫） |
| 事上磨練（事件也長少量四維，隨章節縮放） | 中立／敵方名士（呂布、張角等已移除） |
| **名士基底**（第一回合就生效的專長與加成） | 蜀吳兩包 |
| **經驗值兌換**：四類池、七價格帶、混合消耗 | 道具授予解鎖（`UnlockGrant` FuncType 未做） |
| **12 招 ／ 10 條特質**，解鎖靠名士傳授 | 跨輪的解鎖繼承 |
| 兩層 RNG（保底光階可見 ／ 升階後揭曉） | 寶物系統 |
| 名士 3+3、局內好感度、連動加成 | 收集圖鑑、成就 |
| 12 位名士、解鎖條、記憶碎片 → 初始好感度 | 蜀吳兩包 |
| 名聲／功績作為門檻貨幣 | 音效、美術 |
| 官階文武雙軌（各 12 階）→ **兵量與糧量** | 敵方名士（`enemyNotables` 目前是空的） |
| **4 場七關戰役**，自動戰鬥，走留自選 | 存檔 migration（`skills` → `abilities` 需要一次） |
| 11 種結局（圓夢 6 ／ 中止 4 ／ 在野 1） | |
| 天命商店 9 品項、結算閉環 | |
| 平衡模擬器（7 種策略） | |

---

## 戰役制與養成兌現（本次）

決議與理由見 [RFC-01](docs/RFC-01-campaign-rework.md)（D1–D40）；
契約見 [32 養成兌現](docs/architecture/32_growth_conversion.md)、
[33 戰役](docs/architecture/33_campaign.md)、[23 特質與技能](docs/architecture/23_skill.md)。

### 改了什麼

1. **大檢定 → 七關自動戰役。** 玩家不操作，驗收的是戰前配置：三招 ＋ 三位指揮各一招。
   每一關打完都可以收兵；**沒有及格線**，一關不打也能過章。輸了才夢醒。
2. **鍛鍊產出改成經驗值。** 四類不共用，要在養成畫面花掉它 ——
   買數值（七個價格帶對齊七個等級 G–S）、買特質、買技能。
3. **一切都要先解鎖。** 名士能教的就是他自己表上有的；深關給唯一掉落。
4. **四維改 0–100。** 名士也在同一把尺上，戰前配置畫面四個人可以直接比。
5. **官階的產出從「檢定加值」改成「兵量與糧量」。** 功績從門檻貨幣升級為你的血條。

### 為什麼

舊制的大檢定是一次骰子：六選一實際上是三選一，而三選一是在填一個風險偏好參數。
更糟的是 12 位名士在裡面只貢獻一個 `sortieBonus`（1–8）——
星階、碎片、好感、專長一整套系統塌縮成一個個位數的加法。

而養成那一側缺了中間層：鍛鍊直接寫進 `attributes`，玩家沒有「這些點數要花在哪」的決策。

兩件事必須一起改 —— 戰役需要特質與技能當配置素材，而它們需要一個購買它們的貨幣。

### 兩條在寫程式時才浮現的算式

**傷害也按兵量比例算。** 兵量在 12 階間長 19.8 倍，而四維只有 2 倍上限（0–100）——
傷害若只吃四維就追不上敵方兵力。改成 `兵量上限 × ratio × (attr/50)` 之後整套尺度一致：
**官階＝規模、四維與配置＝倍率，兩者相乘**，所以鍛鍊與委託都不可缺。

**有效血量 = 兵量 + 糧量 ×（政/50）。** 因為恢復按比例、扣同量糧秣，
三條官途在政 = 50 時完全相等（12.3／12.6／12.3），差別只在形狀。

### 實測（模擬器 50 輪／策略）

```
策略               圓夢率   戰役深度      特質  技能
notable-chaser     72%     3.98 關/場     5.8   2.6
flag-chaser        68%     3.60 關/場     5.2   2.6
focus-martial      56%     4.08 關/場     3.8   2.3
focus-civil        48%     3.80 關/場     3.2   2.2
risk-averse(2.4)   76%     —
risk-seeking(1.05) 38%     —
```

### ⚠️ 一個尚未解決的平衡問題

**貪心目前不划算。** risk-averse（要 2.4 倍餘裕才打）拿到 8554 點，
risk-seeking（1.05 倍就上）只有 4765 —— 深入的報酬補不回死掉的損失，
因為死掉損失的是【剩下所有章節】，而一關的獎勵只是功績。

這正是 RFC §6 說模擬器要量出來的東西，現在量到了。可能的方向：
把 `REWARD_MUL` 再拉陡、讓深關的唯一掉落更關鍵、或降低深關的變異
（目前一場仗只有兩三個回合，擲骰的變異相對太大）。**尚未動它** ——
那是 RFC 階段 D 的工作，需要更大樣本。

### 兩處內容債

- 名士的 `unlocks` 文案還在講「大檢定出戰值 +8」「大檢定通過的獎勵 +20%」，
  那些效果（`CheckValueBonus` / `CheckRewardBonus`）在戰役制下已經不會被讀到。
  效果與文案都要改成戰役的語彙。
- `enemyNotables` 全部是空的 —— 「選呂布當玩伴，虎牢關就不能靠他」
  這條機制在程式裡是活的，但沒有內容用它。

---

## 單動作回合制改造（前次）

### 改了什麼

1. **鍛鍊與事件一回合只能選一個**，選完即自動推進到下一回合。
2. **事件除了名聲功績，還給少量四維**（「事上磨練」），隨章節倍率縮放；小檢定失敗仍給四成。

對照《明星志願》：鍛鍊＝上課（長能力），事件＝工作（換錢與名氣，順帶練到一點）。

### 為什麼舊的雙槽制要改

實測下來，**「兩個都選」永遠是最優解**。事件不花任何代價，於是事件槽不是決策
而是免費贈品 —— 整個回合裡只有鍛鍊槽是真正的選擇，門檻貨幣也變成純白賺。

### 結構層變更（不只是規則開關）

| 層 | 變更 |
|---|---|
| contracts | 新增 `TurnAction` / `TurnActionKind` / `ActionTally`；`RunState.actions` 記行動配比；`EventResolution` 去掉 `kind: 'skipped'`；`OptionState` 新增 `practicePreview` |
| 15 章節回合 | 從「推進條件只看鍛鍊槽」改成「一回合恰好一個動作」。`actionOf` 由兩槽各自的回報函式組合而成，**不另存 commitment 欄位** |
| 16 / 17 | 各匯出 `selectedAction` / `resolvedAction`；互斥的守門只在 15 一處（`assertActable`） |
| 17 事件槽 | `skip` 移除；新增 `practiceYield` 純函式（預覽與結算共用一條算式） |
| 資料 | 新增 `eventYieldCurve` 定義族（四維 ＋ 貨幣的章節縮放、`failRatio`） |
| 內容 | 11 則事件、22 個選項全部補上 `practice`；委託原本零星的 `{kind:'attr'}` 獎勵改走 practice |
| 驗證 | 新增 5 條規則（practice 非空／權重正／不重複 attr；兩條成長曲線的長度與單調性；`failRatio ∈ [0,1]`） |
| 模擬器 | 策略介面改為 `chooseAction(s): TurnAction`；策略組改以**事件佔比**為軸線鋪開 |

`LifetimeStats.eventsSkipped` → `actionsTraining` ／ `actionsEvent`：跳過已不是動作，
「回合花在哪一邊」才是這個制度要度量的東西。

### 連帶要改的三張表 ★

這三張表都因為**同一個原因**過期了 —— 它們全部反推自「一個回合同時產出一次鍛鍊
與一次事件」。互斥之後那個前提不成立：

| 表 | 舊值的問題 | 新值的依據 |
|---|---|---|
| 大檢定 DC | 反推自「每回合都鍛鍊」，任何參與事件的玩家都達不到 → 做事等於死 | 量出「三成回合做事」參考打法的實際檢定值（見下方掃描表），三檔取 0.61／0.96／1.31 倍 |
| `careerRank.checkBonus` | `[0,2,4,…,82]` 對照 DC 數百到數千 ≈ 1%，功績換不到檢定力 → 門檻貨幣淪為純懲罰 | `[0,25,50,85,130,190,…]`：在該階自然出現的那一章，加值約為四維檢定值的兩成 |
| `careerInit.byTotalFame` | 門檻 100/200/320/460 反推自「16 回合都做委託」，實際只做得到 6–8 則 → 最高階永遠碰不到 | 減半為 50/100/160/230 |

還順手修掉了兩件事：

- **小檢定 DC 曲線**（`dc:easy/normal/hard`）—— 舊值讓第 1 章的高風險選項恆為 0%
  （HANDOFF 舊版問題 3，見下）。改以「章中的單一相關維」為基準後，第 1 回合就是 31%。
- **事件的貨幣獎勵改乘章節倍率** —— 固定值在乘法縮放的遊戲裡是死內容：
  第 9 章一則委託給 22 功績，對照官階門檻 1830 等於沒給。
- **大檢定的功績獎勵降到約三分之一** —— 一場戰役發出比十則委託還多的功績，
  和「功績＝執行任務累積的服務資歷」（GDD §8.2）矛盾，也讓委託變得可有可無。

---

## 名士系統：八人陣容與基底（前次）

### 改了什麼

1. **收斂成魏的八人。** 移除四位中立名士（呂布、貂蟬、華雄、張角）與呂布事件。
   兒時玩伴池與上司池現在都是同一批八個人。
2. **幼年抽到的，成年不會再抽到** —— 規則本來就在程式裡，但只是 `assignSuperiors`
   裡的一個 `Set`。現在它是文件寫明的不變量、有測試釘住、並且有載入期驗證
   （池的人數必須 ≥ 玩伴數 ＋ 上司數，否則上司會靜靜少於三位）。
3. **名士基底** —— 新增 `NotableDef.base`：專長維、站位加成、專長對位加成、
   專長格站位權重、出戰基底加值。**第一回合就生效，不需要好感度。**

### 為什麼要有基底

舊版的站位加成 `trainingMulByStage` **只看好感度階段，不看是誰**。
於是開局時 ★5 與 ★1 站在格子上數值完全相同 —— 「這格有誰站著」不構成資訊，
玩家沒有理由在意站位，而稀有度要養過才知道意義。

第一回合的實測畫面（改後）：

```
演武  無光  武≈13   夏侯惇·武·陌生 +28%
讀書  銀光  智≈16   —
治事  銀光  政≈20   樂進·武·陌生 +7%　于禁·政·陌生 +15%
交遊  無光  魅≈10   —
```

無光的演武因為夏侯惇對位而值 13；銀光的治事有兩人站著而值 20。
同樣條件下 ★4 對位給 +28%、★1 非對位只給 +7% —— 這是基底在說話。

### 分工：基底 vs 升星

| | 何時生效 | 內容 | 逐人不同 |
|---|---|---|---|
| **base** | 進入陣容第一回合 | 他本來就會的事 | ✅ 依稀有度 ＋ 角色性格 |
| **unlocks** | 好感度／升星達標 | **提升**（經驗 +20%→+40%）＋ **新功能** | ✅ 但要投資 |

### 名士之間【相乘】，而且人越多額外加倍 ★

全員擠進同一格是刻意保留的**爆發時刻**。相加會把那個瞬間壓成平淡的加值，
等於把整套站位 RNG 最好玩的地方拿掉。

**純相乘到不了爆發的量級** —— 實測 400 輪最高只有 ×2.75，因為每人的加成必須夠小
才不會在六人同格時指數爆炸，而那個顧慮反過來壓死了三四人同格的爽感。
所以加了一條同格人數倍率 `[1, 1, 1.15, 1.4, 1.8, 2.3, 3.0]`，
把爆發**只放在人多的時候**：一兩人同格幾乎不受影響。

實測分佈（400 輪）：

```
同格人數（每輪）  0 人 29　1 人 36　2 人 19　3 人 6.1　4 人 1.2　5 人 0.2　6 人 ~0
格子倍率          ×1.0–1.2 61%　×1.5–2.0 13%　×2.0–3.0 5.3%　×3.0–4.0 1.0%　×4.0+ 0.24%
最高倍率          ×6.64（上限 ×8）
```

**約每輪一次會出現 ×3 以上的格子。** 六人同格約每百輪一次 —— 傳說級瞬間。

`tsx scripts/smoke.ts 1234` 看得到一次：

```
R 7 【練】經商 金光 →金光 智+83　★3 人同格 ×1.95★
```

同章其他回合的鍛鍊大約 +20～30。smoke 現在會把三人以上同格標成 `★N 人同格 ×M★`，
爆發時刻在 headless 下也看得見。UI 則在格子上顯示 `×N.NN`，倍率 ≥ 2 時字級放大變金色。

`maxPerSlot` 從 2 改成 6（＝陣容人數）。舊值讓「全員同格」**根本不可能發生**，
爆發感只是空話。新增驗證規則擋住這件事。

### 爆發是誘惑，不是免費的獎賞

| 打法 | 圓夢率 | 輪迴點數 |
|---|---|---|
| train-only | 62.0% | 3021 |
| mix-25（專精 ＋ 兩成辦事） | 60.3% | 3045 |
| mix-50 | 57.5% | **3076** |
| **opportunistic**（好格才練，否則辦事） | 59.5% | 3057 |
| **jackpot-chaser**（永遠練期望值最高的格） | **34.8%** | **1964** |

兩個結果值得記住：

1. **追爆發明顯最差**（1964）。四維被打散（258/232/226/180）過不了大檢定 ——
   「全員擠在交遊格但我需要武」是真兩難，不是免費午餐。
2. **乘法制讓「逐回合看情況」開始值錢了。** 加法制時 opportunistic 輸給固定比例
   （2891 vs 2998）；乘法制之後它反超了全押鍛鍊（3057 vs 3021）。
   好回合與壞回合的差距夠大，讀盤才有報酬 —— 這正是上一輪記下的那個負面結果被解掉了。

### 順帶修掉的一個真實缺陷

**推進到章末大檢定回合時，上一回合的行動會殘留。** `advance()` 在章末不重抽槽位
（要先打大檢定），但也沒清掉 `training.selected` —— 於是「本回合已行動」在一個
還沒行動的回合裡為真，`canAdvance()` 也跟著騙人。

正常流程看不到（UI 會先路由到大檢定畫面），所以它是靠巧合而不是靠規則在正確。
現在 `stepTurn` 無條件清掉上一回合的結算（不消耗 RNG，因此與重抽是兩件事），
並有測試釘住「大檢定回合的 `action` 必須是 null」。

> 這是在瀏覽器裡連點鍛鍊格時發現的 —— React 沒有在連點之間重繪，
> 於是同一個 render 的 handler 被連續呼叫，第二次就撞上了殘留狀態。

### 順帶修掉的東西

- **`role` 欄位移除。** 「UI 分類，不參與任何計算」—— 但連 UI 也沒讀它，
  是假裝成資料的註解。定位現在由 `base.specialty` ＋ 解鎖條組合讀出來。
- **補上 `pol` / `cha` 的光階與檢定效果**（3003/3004/6004/6005）。
  原本只有 war/int，以政或魅為專長的名士無對位解鎖條可掛 ——
  「政與魅是裝飾」有一半是這裡缺料造成的。
- **四維各需至少一位專長者**：新增驗證規則。

### 專長分佈是陣營性格，不是缺漏

```
武 夏侯惇・典韋・樂進　智 郭嘉・程昱　政 荀彧・于禁　魅 曹操
```

魏只有主公一人以魅為專長 —— 曹魏重才幹輕人望。代價是「交遊」格在多數 Run
裡沒人站。補蜀（劉關張）時人望型會自然補上。

### 平衡影響（500 輪／策略）

| 策略 | 事件佔比 | 圓夢率 | 輪迴點數 |
|---|---|---|---|
| train-only | 0% | 61.4% | 2924 |
| mix-25 | 14% | 59.2% | 2957 |
| mix-50 | 43% | 55.8% | **2998** |
| opportunistic（看情況決定） | 22% | 58.6% | 2891 |

峰值從改造前的 **兩成** 移到 **四成**（更接近你要的五五分），而且 0–50%
的點數差只有 2.5% —— 事件佔比現在幾乎是**風格選擇**而非最優解問題。

**一個負面結果（已在後續解掉）**：加法制時 `opportunistic` 沒有贏過固定比例
（2891 vs 2998）—— 它為了等好回合而少練兩成回合，而「等一個好回合」的期望值
補不回少練的量。改成乘法制之後好回合的價值被放大，它反超了全押鍛鍊
（3057 vs 3021）。**「讓好回合更好」這條路是對的。**

---

## 灰盒實測抓到的三個設計問題

### 1. 加法骰在數值放大後會殺掉「難度自選」★ 已修

原設計是 `檢定值 + d100`。實測結果：

```
【虎牢關】 穩 100%　│　進 19%　│　險 0%
【官渡】   穩 100%　│　進 0%　│　險 0%
【平定河北】穩 100%　│　進 100%　│　險 0%
```

檢定值到 700 時，±50 的擺幅只佔 7%，成功率全部塌成 100% 或 0% —— **難度自選不再是決策**。

**已改為比例擺幅**：`total = 檢定值 × (1 + (roll − 50) / 100)`，倍率 0.51–1.50。修正後：

```
【官渡】   穩 93%　│　進 25%　│　險 0%
【平定河北】穩 88%　│　進 32%　│　險 0%
```

> ⚠️ 這是**架構層變更**（18 §3 的算式形狀），已同步 `CheckRuleDef`（新增 `rollCenter` / `rollSpread` / `baseFloor`）。
> `baseFloor` 是比例骰逼出來的：第 1 回合四維全 0，沒有下限的話任何檢定都是 0%。

### 2. 門檻不寫，事件槽永遠是滿的 ★ 已修

居民委託原本 `requirements: []` 全空，於是事件槽永遠 3 個，「門檻決定菜單豐富度」這個設計根本沒被行使。加上名聲與官階門檻後：

```
事件槽 平均 2.13–2.58 個　滿 3 個 12–58%
```

早期少、養起來才多 —— 這正是預期的教學曲線形狀。

### 3. 小檢定的「進」難度在第 1 章不可達 ★ 已修

實際遊玩時看到：

```
託書　如期送到 79%　│　先拆開看看寫了什麼 0%
```

高風險選項在第 1 章永遠 0%，因為它走 `dc:normal` 曲線而角色四維還是 0（吃 baseFloor 25）。**選項存在但永遠不能選，等於死內容。**

**已改**：三條曲線改以「章中的單一相關維」為基準（ch1 基準 30，依 `chapterMultiplier`
的累積比放大），三檔取 0.6／1.0／1.35 倍。第 1 回合實測：

```
託書　如期送到 79%　│　先拆開看看寫了什麼 31%
```

單動作回合制讓這條曲線比以前重要得多：事件要花掉一整個鍛鍊回合，
若成功率不可及，玩家就不會去做事，整條事件線會空轉。

---

## 模擬器目前的數字

`npm run sim -- 300`。策略組已改以**事件佔比**為軸線鋪開（鍛鍊的挑格方式固定為
專精主檢定屬性，否則「押單維 vs 平均分配」會蓋掉事件佔比的影響）：

| 策略 | 事件佔比 | 圓夢率 | 通過章節 | 輪迴點數 | 最常見結局 |
|---|---|---|---|---|---|
| train-only（只上課） | 0% | 65.3% | 3.10 | 2838 | **有成之人 46.7%**（兜底稱號） |
| **mix-25** | 14% | 65.0% | 3.12 | **3090** | **將軍 46.0%** |
| glow-gated（光階差就去做事） | 44% | 52.0% | 2.57 | 2831 | 將軍 41.0% |
| mix-50 | 43% | 42.7% | 2.32 | 2466 | — |
| mix-75 | 70% | 12.7% | 0.91 | 1192 | — |
| event-only（只打工） | 100% | **0.0%** | 0.00 | 416 | 戰歿 100% |

**這張表就是這次改造要看的東西。** train-only 與 mix-25 圓夢率幾乎相同（65%），
但結局差一整級：只上課的人以「有成之人」（兜底稱號、點數 ×1.2）收場，
分兩成回合去辦事的人當上「將軍」（×1.4）並多拿 250 點。

兩端都比中間差 —— 只練過不了官場，只做事過不了大檢定。**曲線有峰，事件不是被支配的選項。**

### 事件佔比掃描（固定「穩」難度，排除代理的冒險偏好）

| 事件佔比 | 通過章節 | 圓夢率 | 輪迴點數 | 官階 文/武 | 各章「穩」檢定值 |
|---|---|---|---|---|---|
| 0% | 3.93 | 97% | 3507 | 2.0/2.0 | 133 / 356 / 289 / 834 |
| **25%** | 3.81 | 91% | **4067** | 2.0/2.9 | 118 / 314 / 262 / 779 |
| 33% | 2.96 | 61% | 3278 | 1.8/3.5 | 104 / 252 / 237 / 669 |
| 50% | 2.67 | 53% | 2986 | 1.7/3.4 | 88 / 231 / 217 / 663 |
| 75% | 0.85 | 13% | 1278 | 1.3/1.9 | 55 / 174 / 182 / 584 |
| 100% | 0.00 | 0% | 416 | 1.0/1.0 | 31 |

第四欄就是新版大檢定 DC 的反推來源（33% 那一列）。

光階實測分佈 `none 38.7% / silver 36.4% / gold 18.4% / red 6.5%`，對照設計保底值 `45/35/16/4` —— 差異來自第二層升階，**兩層 RNG 如預期在運作**。

一個意外的好性質：**官渡（智為主）的檢定值低於前一章的虎牢關（武為主）**。因為前兩章都是武為主，一路追武的玩家到智為主的官渡就吃虧。**主屬性在章節間變化本身就是對純專精的懲罰**，不需要額外設計 —— 也因此各章的 DC 只能跟同一個主屬性的成長比，不能跨章橫向比較。

---

## 所有 GREYBOX 數值的位置

全部在 `content-source/core/config/`，改完跑 `npm run content:build`：

| 檔案 | 內容 |
|---|---|
| `glow-tiers.ts` | 光階倍率與保底權重 |
| `aptitude.ts` | 資質七階的位移與倍率、累計成本 |
| `training.ts` | 四維基礎值、章節倍率、升階基礎機率、`shiftStepRatio`、**`eventYieldCurve`（事件的四維產出比與 `failRatio`）** |
| `affinity.ts` | 好感度階段、碎片成本曲線、**階段加成**（改為加成，不再是倍率） |
| `notable-base.ts` | **名士基底的稀有度基準**（站位加成、對位加成、站位權重、出戰加值） |
| `rules.ts` | 四維上限、善惡名上下限、事件上限、檢定骰參數 |
| `../career/ranks.ts` | 官階功績門檻、**`checkBonus`（門檻貨幣換檢定力的唯一管道）**、入朝初始階級 |
| `../events/pools.ts` | 小檢定 DC 三條曲線、委託參數池 |
| `../chapters/nanhua.ts`、`../../wei/chapters.ts` | 大檢定 DC |
| `../shop/index.ts` | 商店定價 |
| `../meta/settlement.ts` | 結算係數 |

**`shiftStepRatio`（目前 0.18）對應 GDD §5.2 的「±N 檔」語意，仍是待定平衡問題**（ARCHITECTURE §9-2）。

**單動作回合制的核心旋鈕只有一個**：`eventYieldCurve.baseByAttr`（目前 4）
對 `trainingCurve.baseByAttr`（目前 10）的比值 —— 事件的四維產出約為鍛鍊的三成。
調高會讓鍛鍊失去意義，調低則會讓做事等於落後。改動後務必重跑事件佔比掃描，
確認曲線仍然**有峰**。

---

## 門禁抓到過什麼

紀律門禁上線時抓到 **11 筆真實違規**，包含我自己寫的跨 slice 直接讀取。修的過程逼出一個規則細化：

> **座標型 slice**（`progress` / `faction` / `config` / `metaSnapshot` / `seed`）全域可讀 —— 它們是「這一輪是誰、走到哪」的識別，不是累積的領域數值。
> **領域型 slice**（`attributes` / `currencies` / `career` / `roster` / `slots` / `charges` / `ending`）必須經擁有者的 Query 介面。

把兩類混為一談，規則會逼出一堆只為了過門禁而存在的 getter。這條比 `interfaces.md §8` 的初稿精確，應回寫該文件。

內容驗證也在調校過程中擋下我自己：把 DC 暫時設成 1 想量分佈時，`DC 必須沿 safe→normal→hard 嚴格遞增` 直接失敗。

---

## 下一步的優先序

1. **玩它。** 手感是我交不了的部分 —— 「這回合要練還是要辦事」的糾結夠不夠強、
   兩層 RNG 的抽獎瞬間、事件文本讀不讀得下去。
   特別要看：**選完就跳**的節奏會不會讓人來不及看清結果（回合紀錄夠不夠用）。
2. **判定兩個我不該替你決定的平衡問題**：
   - 目前峰值在事件佔比 **兩成**。若你希望「上課／工作」更像五五分，
     要動的是 `eventYieldCurve.baseByAttr`（4 → 5～6）或再放寬大檢定 DC。
   - 灰盒只有 **2 章陣營線**，但官階梯是照 7 章設計的（爬到 12 階要 1830 功績）。
     所以「靠委託升官」在灰盒裡只跑得到 rank 5–6。補足陣營章節後這張表要重測。
3. **內容缺口（上一輪量出來的，尚未動）**：
   - 委託模板只有 4 個，其中 2 個被文名門檻鎖住 → 託書與流寇之患各被抽出 ~20 次/輪
   - 居民委託的門檻全掛在**文名**，而追武的主流打法產**武名** → 那條線空轉
   - 名士事件抽出 0.13–0.45 次/輪（要 friendly，但名士席次只有 19% 到得了）
   - 5/11 結局從未達成（官階天花板 文 4 / 武 4，但那三個要 5–6 階）
4. 補技能系統的內容（架構已備，見 `docs/architecture/23_skill.md`）
5. 蜀吳兩包 —— 驗收標準是 `pack:core` 一行不動
