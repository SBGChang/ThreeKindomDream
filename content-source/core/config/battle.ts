import type { BattleRuleDef } from '../../../src/contracts/core/definitions.js';
import { coreDef } from '../pack-id.js';

/**
 * 戰役的規則（33）★ 全部 GREYBOX。
 *
 * ── 主角的施放次數 ──────────────────────────────────
 * `[1, 0.6, 0.3]` ＝ 保底一招 ＋ 第二招 60% ＋ 第三招 30%，三次獨立擲：
 *
 *   1 招 28% ／ **2 招 54%** ／ 3 招 18%　→ 期望 1.9 招／回合
 *
 * 第一項必須是 1 —— **保底一招**是機制本體（下限由玩家自己保證），
 * 由載入期驗證強制。
 *
 * 而「先擲次數、再抽哪一招、同回合不重複」讓三格的期望上場率相同
 * （各約 0.63 次／回合）。於是配置從「排優先序」變成【設計一組組合】——
 * 三招之間必須各自成立，因為你不知道哪一招會出。
 *
 * ── 指揮傳令的機率 ──────────────────────────────────
 * 三人同階的期望：相識 0.45 ／ 友好 0.90 ／ 知交 1.50 ／ 莫逆 2.10。
 * 加上主角的 1.9，從相識到莫逆【一回合的行動數幾乎翻倍】，
 * 而且過半數的行動來自別人 —— 好感因此不再是一個百分比加成，
 * 而是「你的軍隊每回合能做幾件事」。
 *
 * `baseStartAffinity = 20` 表示所有人開局就在相識，因此第一場戰役
 * 就偶爾有人傳令來，不會是死的。
 *
 * ── 敵方曲線：索引【章節】，數字從【預期能力值】反推（D64）★★ ──
 *
 * 這條索引改過兩次。第一版索引官階（D25），理由抄自 17 §6.4
 * 「難度與報酬必須一起長」—— 但那條規矩講的是委託報酬，
 * 類推到敵人身上沒有依據。而它有實測的壞處：官階一旦被本輪上限封住，
 * 敵人跟著凍住，玩家卻照樣練上去 —— 第一輪就輕鬆打到第七關。
 *
 * **也不能改成「執行期讀四維」**：那是等級同步，練越高對面越強，
 * 養成整個白做。對標必須發生在【設計時】—— 就是下面這張表。
 *
 * ── 對標表（scripts/benchmark.ts，25 輪／組）★ ────────
 * 走到每一章的戰役、配置完成那一刻，玩家實際帶著什麼：
 *
 *   星階  章   主維   兵量   有效軍勢   每回合輸出
 *    無   1     37    188      401         103
 *    無   2     47    270      546         199
 *    無   3     56    368      782         332
 *    無   4     68    527     1226         627
 *    5★  4     75    815     2012        2778
 *
 * 「有效軍勢」＝ 兵量 ＋ 糧秣換得回的（§5.3）。
 * **戰力預算 ＝ 有效軍勢 × 每回合輸出** —— 這一章總共吸收得起多少傷害。
 *
 * ── 反推 ───────────────────────────────────────────
 * 打完第 K 關累計吃到的傷害：
 *
 *   Σ (敵兵ᵢ / 我方輸出) × 敵輸出ᵢ
 *     ＝ (兵力基準 × 輸出基準 / 我方輸出) × Σ TROOPS_MULᵢ·DAMAGE_MULᵢ
 *     ＝ (兵力基準 × 輸出基準 / 我方輸出) × cum(K)
 *
 * 令它等於有效軍勢，解出【兵力基準 × 輸出基準】。
 * 目標 K ＝ **無星第一輪停在第 2–3 關**，取 cum(2.5) ＝ 1.058（build.ts）。
 * 再用「平手關（TROOPS_MUL 1.0）打五回合」把乘積拆成兩條：
 *
 *   兵力基準 ＝ 5 × 我方輸出
 *   輸出基準 ＝ 有效軍勢 / (5 × cum(2.5)) ＝ 有效軍勢 / 5.29
 *
 * ── 校準常數 k ＝ 0.23 ★★ 閉式解漏掉的東西都收在這裡 ─────
 * 上面那條解出來的輸出基準【實測太重】：無星只打到 0.70 關。
 * 因為閉式解漏了三件都在加重玩家負擔、而且都難以解析地寫進去的事：
 *
 *   1. 關底敵將**每回合多打一下**，而且那一下也乘 dmgBase
 *   2. 兵量掉下去我方輸出跟著掉 —— 死亡螺旋。閉式解假設輸出恆定
 *   3. 「有效軍勢」裡的糧秣要花招式格去換，那些回合不輸出
 *
 * 所以：**形狀（章節間的比例、關與關的斜率）由推導決定，
 * 絕對高度由這一個常數決定。** 掃描量出來的（scripts/calibrate.ts）：
 *
 *     k      深度（無／3★／4★／5★）    七關全清率（無／3★／4★／5★）
 *   **0.23    2.92  5.96  6.38  6.69**   2%  54%  68%  82%  ← 採用
 *   0.20    3.26  6.21  6.58  6.78     2%  63%  75%  86%
 *   0.18    3.59  6.50  6.74  6.83     4%  73%  81%  88%
 *
 * 往下（更鬆）能把 4–5★ 推到八成五，但**無星會先漂出「2–3 關」那個帶** ——
 * 兩端不能同時滿足時，優先守第一輪：那是使用者講得最明確的一條。
 *
 * ── 為什麼看全清率而不是平均深度 ★ ──────────────────
 * 玩家講的是「**穩定**過第七關」。平均深度會被 7 這個天花板壓平
 * （5★ 從 6.68 到 6.87 看起來只差一點，全清率卻是 81% → 92%），
 * 所以真正要對的指標是全清率。「大部分四星、有的五星」落在
 * 74% 與 86% 之間 ≈ **八成**，那就是「穩定，但不是保證」。
 *
 * 而無星是 **0%** —— 第一輪連一次七關全清都不會發生。
 * 那道牆存在，而且它是靠養成拆的，不是靠運氣。
 *
 * 下面那排輸出 ＝ 推導值 × 0.23。**不要單獨改其中一格** ——
 * 要嘛改 k（整條難度的高度），要嘛改對標表（章節間的比例），
 * 要嘛改 build.ts 的 g（關與關的斜率，那條管的是「星階買得動幾關」）。
 *
 * ── 這張表會隨著【功績階梯】一起動 ★ 別把它當常數 ────
 * 兵量來自 hostScale[名義階]，名義階來自功績。所以 career/ranks.ts
 * 一改，這張表就要用 `scripts/benchmark.ts` 重量一次 ——
 * 上一版的表（兵量 191/312/452/685）就是這樣過期的。
 *
 * 第五章之後沒有內容，按實測的每章 ×1.83 兵力 ／ ×1.45 輸出外推。
 */
export const battleRule: BattleRuleDef = coreDef('battleRule', 'battle:main', {
  castChances: [1, 0.6, 0.3],
  commandChanceByStage: {
    stranger: 0, acquainted: 0.15, friendly: 0.30, close: 0.50, sworn: 0.70,
  },
  troopsBase: 120,
  supplyBase: 120,
  // 沿用 `trainingCurve.crossLineRatio` 的形狀與數值。0.5 那一項自帶
  // 防退化底線：純武官的糧量有近八成來自他自己的武官階，
  // 因此「零糧秣」不可能出現 —— 不需要另加基底常數。
  crossLineRatio: 0.5,
  // 0–100 尺度下 50 ＝ ×1.0、100 ＝ ×2.0。四維買的是【倍率】，
  // 官階買的是【規模】，兩者相乘 —— 所以兩邊都不可缺。
  actorDivisor: 50,
  /**
   * 敵方兵力 ＝ 一關要打幾回合 ★
   *
   * 反推：平手關（troopsMul 1.0）要打【約五回合】。
   * 我方每回合輸出 ≈ 兵量上限 × 招的 ratio × (主維/50) × 1.9 招 ＋ 指揮，
   * rank 1 約 138、rank 7 約 1250 —— 乘五就是這條曲線。
   *
   * ── 為什麼要五回合而不是兩回合 ★★ ────────────────
   * 這是這一版最重要的一個數值決定。
   *
   * 兩回合的仗，勝負幾乎完全由「這回合放了幾招」（1／2／3，28%／54%／18%）
   * 決定 —— 擲骰的變異比估計值本身還大。於是「算得剛剛好就上」實際上
   * 是輸一半，貪心因此永遠不划算：實測 risk-seeking 只拿到
   * risk-averse 的 56% 點數（4765 對 8554）。
   *
   * 五回合之後大數法則開始作用，估計值變得可信 ——
   * **玩家算得出來的東西，才值得他去算。** push-your-luck 的整個前提
   * 就是「你可以判斷」，而判斷要能成立，變異就不能蓋過訊號。
   *
   * 代價是戰鬥變長（滿七關約 55 回合）。掃蕩與戰報預設收起就是為了它。
   */
  enemyTroopsByChapter: [
    515, 995, 1660, 3135, 5740, 10500, 19200, 35200, 64400,
  ],
  /**
   * 敵方每回合輸出 ＝ 一關要掉多少血 ★ ＝ 有效軍勢 / 5.29（見上方反推）
   *
   * 與兵力【分開訂】，兩條各管一件事：
   *   兵力 → 一關要打幾回合（訂在五回合，那是估算得以成立的前提）
   *   輸出 → 一關要掉多少血
   *
   * 乘積才是「這一關的代價」，反推也是對乘積做的 ——
   * 拆開之後兵力先滿足五回合，輸出拿剩下的。難度旋鈕在這一條：
   * 要更【難】而不要更【長】，就只能動它（動兵力會把仗拖長）。
   *
   * 數字 ＝ 有效軍勢 / (5 × cum(2.5)) × 校準常數 0.23。
   *
   * ── 實測（無星第一輪、greedy-gain、30 輪）★ ─────────
   *   平均打到 **2.92 關**、七關全清率 **2%**。
   * 正是玩家要的「一開始大概只能打到 2、3 關就打不下去」。
   */
  enemyDamageByChapter: [
    19, 25, 36, 57, 83, 120, 174, 252, 366,
  ],
  // 恢復 1 點軍勢消耗 1 點糧秣 —— 於是【糧量就是「你能把軍隊填回多少」】。
  // 有效血量 = 兵量 + 糧量 ×（政 / 50），三條官途在政 = 50 時完全相等，
  // 差別只在形狀（33 §5.3）。
  supplyPerTroop: 1,
  rallyRatio: 0.35,
  // 安全閥，不是玩法上的回合上限（D9 明確不做那個）：
  // 若玩家一招輸出都沒有，戰鬥不能無限跑。
  // 第七關在平手時約 16 回合，所以上限要留得比它寬很多。
  maxTurns: 120,
  // 掃蕩只吃「不需要想的那幾關」。4 倍餘裕 ≈ 前三關 ——
  // 七場自動戰鬥第一輪好看、第五輪是阻礙，這條就是為那件事存在的。
  sweepMargin: 4,
});
